#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);
    int h,f; cin >> h >> f;
    if(h>f) cout << "flight";
    else cout << "high speed rail";
    return 0;
}