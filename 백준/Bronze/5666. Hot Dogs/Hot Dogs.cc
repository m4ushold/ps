#include <bits/stdc++.h>
using namespace std;
using ll=long long;

int main() {
    ios::sync_with_stdio(0); cin.tie(0);
    int h,p;
    while(cin >> h >> p) cout << fixed << setprecision(2) << 1.*h/p << '\n';
    return 0;
}