#include <bits/stdc++.h>
using namespace std;
using ll=long long;

int main() {
    cin.tie(0)->sync_with_stdio(0);
    int t; cin >> t;
    while(t--) {
        double a; cin >> a;
        cout << fixed << setprecision(10) << a/6 << '\n';
    }
    return 0;
}