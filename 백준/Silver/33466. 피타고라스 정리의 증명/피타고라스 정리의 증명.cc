#include <bits/stdc++.h>
using namespace std;
using ll=long long;

int main() {
    cin.tie(0)->sync_with_stdio(0);
    ll t; cin >> t;
    while(t--) {
        ll n; cin >> n;
        cout << n+n/2*2 << '\n';
    }
    return 0;
}